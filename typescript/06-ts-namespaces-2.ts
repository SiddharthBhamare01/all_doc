///<reference path="06-ts-namespaces.ts"/>

namespace ibm{
    export const mulNums=(a:number,b:number)=>{console.log(a*b);};
    export const divNums=(a:number,b:number)=>{console.log(a/b);};
    
    
    mulNums(20,10);
    divNums(20,10);
}
